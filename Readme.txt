What I would had I more time?:
- Decouple code from MonoBehaviours into pure c# classes.
- Add plenty more test cases.
- Fix UI pixels to be according to specs.

Known issues:
- UI pixels aren't according to specs.
- Health bar isn't with a round top(need to find sprite).